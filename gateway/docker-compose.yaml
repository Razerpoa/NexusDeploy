services:
  gateway:
    image: nginx:latest
    container_name: nexus-gateway
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./conf.d:/etc/nginx/conf.d:ro
    # Run host network meaning we bind directly to ports 80/443 on the host
